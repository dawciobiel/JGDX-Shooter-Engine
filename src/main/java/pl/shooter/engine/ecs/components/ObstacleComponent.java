package pl.shooter.engine.ecs.components;

import pl.shooter.engine.ecs.Component;

/**
 * Marks an entity as a physical obstacle on the map, blocking movement and pathfinding.
 */
public class ObstacleComponent implements Component {
    public ObstacleComponent() {}
}
